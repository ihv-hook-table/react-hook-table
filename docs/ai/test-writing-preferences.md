# Test Writing Preferences

Use the repository's established test style first when adding or changing tests.
For new tests, never use type casting, always run `vp fmt` on newly created files, and typecheck the new tests and fix any resulting errors.

For this project, the canonical test-writing rules live in `docs/ai/test-writing-skill.md`.
