/* eslint-disable @typescript-eslint/no-unused-vars */

import { ExpandableRowProps, TableRecord } from '../../types';

export const Expandable = <T extends TableRecord = TableRecord>(
  _props: ExpandableRowProps<T>,
): JSX.Element => {
  return <></>;
};
